# 🚀 Оптимизация производительности (Production)

Документация по оптимизациям для **реального сайта после деплоя** на GitHub Pages.

---

## ⚠️ Важно понять: Static Export

Ваш проект использует **`output: 'export'`** в `next.config.ts`. Это значит:

1. **Server Components выполняются ТОЛЬКО при билде** (`npm run build`)
2. **Генерируются статические HTML файлы** в папку `out/`
3. **Нет серверного рендеринга** на продакшене
4. **Сайт работает как SPA** (Single Page Application) после первой загрузки

---

## ✅ Что РЕАЛЬНО работает в production

### 1. Prefetch для мгновенных переходов
**Файл:** `components/cases-grid.tsx`

**Что добавлено:**
```tsx
<Link href={href} prefetch={true} className="block h-full w-full">
  {content}
</Link>
```

**Как работает в production:**
- При попадании Link в viewport (видимую область) → **автоматически загружается HTML следующей страницы**
- При клике → переход **мгновенный**, так как HTML уже загружен
- Для статических роутов загружается **весь роут полностью**

**Важно:** Работает **только в production** (`npm run build && npm start` или после деплоя).

---

### 2. View Transitions для плавной анимации
**Файл:** `app/globals.css`

**Настройки:**
```css
/* Старая страница исчезает мгновенно */
::view-transition-old(root) {
  animation: none;
  opacity: 0;
}

/* Новая страница плавно появляется */
::view-transition-new(root) {
  animation: fade-in 0.4s ease;
}

@keyframes fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

**Как работает:**
- Это **клиентская технология** — работает в браузере
- Не зависит от сервера или билда
- Работает везде: dev, production, GitHub Pages

**Параметры:**
- **Длительность:** 0.4s
- **Timing:** ease (плавное начало и конец)
- **Эффект:** fade-in новой страницы

---

### 3. Client-side навигация (SPA режим)
**Файл:** `app/layout.tsx`

**Что используется:**
```tsx
import { ViewTransitions } from 'next-view-transitions'
import { Link } from 'next-view-transitions'
```

**Как работает:**
- После первой загрузки сайт работает как **SPA**
- Переходы между страницами **не перезагружают** всю страницу
- Загружается только новый HTML и подменяется контент
- Это **намного быстрее** обычных переходов

---

## ❌ Что НЕ нужно для static export

### React cache() — бесполезен
```tsx
// ❌ НЕ НУЖНО для static export
import { cache } from "react"
const getData = cache(async () => { ... })
```

**Почему:**
- При `output: 'export'` Server Component выполняется **один раз при билде**
- Кеширование между запросами не имеет смысла — запросов нет!
- Файл читается один раз при `npm run build` и всё

---

## 🎯 Итоговая производительность

### Что происходит после деплоя:

1. **Первая загрузка страницы:**
   - Браузер загружает HTML, CSS, JS
   - Скорость зависит от GitHub Pages CDN

2. **Клик на Link с prefetch={true}:**
   - HTML уже загружен в фоне → **переход мгновенный** (0ms задержка)
   - View Transitions показывает fade-in анимацию (0.4s)

3. **Визуально:**
   - Пользователь видит **моментальный переход**
   - Плавное появление новой страницы
   - Нет "белого экрана" или задержек

---

## 🔧 Как тестировать production локально

```bash
# Собрать production build
npm run build

# Запустить локальный сервер для static файлов
npx serve out
```

Откройте http://localhost:3000 и проверьте:
- ✅ Переходы мгновенные
- ✅ Prefetch работает (смотрите в Network tab)
- ✅ View Transitions плавные

---

## 📊 Оптимизации сборки

### Turbopack для быстрого билда
**Файл:** `package.json`

```json
{
  "scripts": {
    "build": "next build",
    "dev": "next dev"
  }
}
```

**Результат:** Сборка проекта быстрее (Turbopack вместо Webpack).

**Важно:** Это влияет на **скорость билда**, не на скорость сайта.

---

## 🎨 Настройка анимации переходов

Если нужно изменить скорость или стиль:

**Файл:** `app/globals.css`

```css
/* Быстрее (0.2s) */
::view-transition-new(root) {
  animation: fade-in 0.2s ease;
}

/* Медленнее (0.6s) */
::view-transition-new(root) {
  animation: fade-in 0.6s ease;
}

/* Другой timing */
::view-transition-new(root) {
  animation: fade-in 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}
```

---

## 📌 Чеклист для новых страниц

При добавлении новой страницы:

1. ✅ Используйте `Link` из `next-view-transitions`
2. ✅ Добавьте `prefetch={true}` для важных ссылок
3. ❌ НЕ используйте `cache()` — это бесполезно для static export
4. ✅ Server Components можно использовать — они выполнятся при билде

**Пример:**
```tsx
import { Link } from 'next-view-transitions'

// ✅ Правильно
<Link href="/page" prefetch={true}>
  Перейти
</Link>

// Server Component — выполнится при билде
export default async function Page() {
  const data = await readFile('data.json', 'utf8')
  return <div>{data}</div>
}
```

---

## 📚 Документация

- [Next.js Static Exports](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)
- [Link prefetch](https://nextjs.org/docs/app/api-reference/components/link#prefetch)
- [View Transitions API](https://developer.mozilla.org/en-US/docs/Web/API/View_Transition_API)
- [next-view-transitions](https://github.com/shuding/next-view-transitions)

---

**Дата:** 2026-02-06
**Next.js:** 16.1.6
**Режим:** Static Export (`output: 'export'`)
